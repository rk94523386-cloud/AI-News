// Backend build script for Vercel
// Backend is deployed independently and runs directly with tsx
// No compilation needed - dependencies are installed via npm

console.log("✓ Backend ready for Vercel deployment");
console.log("  - Will run with: tsx index.ts");
console.log("  - Serving on: $PORT environment variable");
