# Backend Environment Variables

# Port for the backend server
PORT=5000

# Node environment
NODE_ENV=development

# Optional: Skip Vite middleware (useful if Vite initialization fails)
# SKIP_VITE=true
